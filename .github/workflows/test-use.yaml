name: test action
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: neilkuan/ecspresso@v2.8.0
        with:
          version: v2.8.0
      - run: |
          ecspresso version 
          ecspresso diff --help